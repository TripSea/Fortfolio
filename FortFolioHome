<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fortfolio - Guard Your Capital, Grow Your Wealth</title>
    <style>
        /* CSS Variables for Theme */
        :root {
            --primary-color: #D4AF37; /* Gold */
            --background-color: #000000; /* Black */
            --text-color: #FFFFFF; /* White */
            --accent-color: #FFD700; /* Brighter Gold for accents */
            --font-heading: 'serif'; /* Elegant serif for headings */
            --font-body: 'sans-serif'; /* Clean sans-serif for body */
        }

        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-body);
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Typography */
        h1, h2, h3 {
            font-family: var(--font-heading);
            color: var(--primary-color);
        }

        a {
            color: var(--accent-color);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        a:hover {
            color: var(--primary-color);
        }

        /* Header/Hero Section - Matching the Visual */
        .hero {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
            padding: 2rem;
            position: relative;
            animation: fadeIn 1s ease-in-out;
        }

        .hero::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: var(--background-color);
            z-index: -1;
        }

        .logo {
            max-width: 300px;
            margin-bottom: 1rem;
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .tagline {
            font-size: 1.5rem;
            color: var(--primary-color);
            margin-bottom: 2rem;
        }

        .cta-button {
            background-color: var(--primary-color);
            color: var(--background-color);
            padding: 1rem 2rem;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .cta-button:hover {
            background-color: var(--accent-color);
            transform: translateY(-3px);
        }

        /* Sections */
        section {
            padding: 4rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .services {
            background-color: #111111; /* Slightly lighter black for contrast */
        }

        .services-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        .service-item {
            background-color: #222222;
            padding: 2rem;
            border-radius: 8px;
            transition: box-shadow 0.3s ease;
        }

        .service-item:hover {
            box-shadow: 0 4px 8px rgba(212, 175, 55, 0.3);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 1rem;
            background-color: #111111;
            font-size: 0.8rem;
            color: var(--primary-color);
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Responsive Design - Mobile First */
        @media (min-width: 600px) {
            .hero {
                flex-direction: row;
                justify-content: space-between;
            }

            .logo {
                max-width: 400px;
            }

            .services-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (min-width: 1024px) {
            .services-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }

        /* Accessibility and Performance */
        img {
            max-width: 100%;
            height: auto;
            display: block;
        }

        /* Placeholder for Attached Visuals */
        /* Replace src with actual image URLs from your attachments */
    </style>
</head>
<body>
    <header class="hero">
        <img src="path/to/fortfolio-logo.png" alt="Fortfolio Logo" class="logo"> <!-- Use attached banner image -->
        <div>
            <h1>Fortfolio</h1>
            <p class="tagline">Guard Your Capital, Grow Your Wealth</p>
            <button class="cta-button">Get Started</button>
        </div>
        <img src="path/to/ff-shield.png" alt="FF Shield Emblem" class="logo" style="max-width: 200px;"> <!-- Use attached shield image -->
    </header>

    <section class="services">
        <h2>Our Services</h2>
        <div class="services-grid">
            <div class="service-item">
                <h3>Portfolio Management</h3>
                <p>Customized asset allocation to balance risk and returns.</p>
            </div>
            <div class="service-item">
                <h3>Risk Protection</h3>
                <p>Strategies to mitigate market volatility and protect capital.</p>
            </div>
            <div class="service-item">
                <h3>Wealth Growth</h3>
                <p>Long-term planning for sustainable financial expansion.</p>
            </div>
        </div>
    </section>

    <section class="about">
        <h2>About Fortfolio</h2>
        <p>Fortfolio LLC is dedicated to providing secure financial advisory services, ensuring your capital is guarded while fostering growth.</p>
    </section>

    <section class="contact">
        <h2>Contact Us</h2>
        <p>Email: info@fortfolio.com | Phone: (123) 456-7890</p>
    </section>

    <footer>
        "Fortfolio" is a trademark of Fortfolio LLC. © 2025 Fortfolio LLC. All rights reserved.
    </footer>
</body>
</html>